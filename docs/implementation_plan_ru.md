# MEMRYTH: практический план реализации

Дата фиксации: 2026-05-01

Обновление статуса: 2026-05-07

Закрыто после фиксации плана:

- экспорт всей библиотеки в JSON и системный share;
- импорт JSON в режиме merge без перезаписи существующих записей;
- отдельный раздел "Данные и резервная копия" в настройках;
- дата последнего полного экспорта;
- пустой приватный старт без автоматического demo seed;
- onboarding первого запуска;
- Android package id, namespace, release signing и release build;
- privacy policy, Google Play draft и in-app privacy policy;
- share-to-app, quick add shortcut;
- PIN lock и biometric unlock скрыты из beta UI до recovery-модели;
- обзорный экран MEMRYTH Pro;
- каркас Google Play Billing / Pro entitlement через `in_app_purchase`;
- Android home widget для quick add;
- batch selection, batch favorite/unfavorite и batch topic assignment;
- export selected через тот же совместимый JSON backup-формат;
- удалены saved filters: функция оказалась лишней для beta и путала основной
  сценарий фильтрации.
- UX-проход первого запуска:
  - пустая библиотека показывает первое действие;
  - фильтры типов стали `Все / Цитаты / Мысли / Фрагменты / ★`;
  - форма записи использует `Текст цитаты`, `Текст мысли`,
    `Текст фрагмента`;
  - добавлен визуальный блок `Контекст`;
  - лишнее отдельное поле источника удалено из модели, Hive adapter,
    JSON export/import, demo seed и тестов;
  - `Аккаунт / Синхронизация` убраны из настроек до реальной реализации.

Осталось после текущего прохода:

- создать реальный managed product в Play Console и проверить purchase/restore;
- `QuoteSortMode` вынесен в `lib/models/quote_sort_mode.dart`;
- пустой `QuoteListViewModel` удален, `QuotesScreen` использует `QuoteController` напрямую;
- `lib/controllers` уже используется вместо старого пути;
- расширить Android widgets: случайная избранная запись / resurfacing;
- saved collections / resurfacing;
- будущий subscription-слой только при появлении sync/cloud/AI/web clipper;
- Pro v1 пересобран вокруг Review Mode, Smart Resurfacing, Collections,
  красивого экспорта и Backup History; обычные batch actions остаются free.
- ручной beta-smoke на телефоне выполнен, результаты перенесены в
  `docs/beta_smoke_findings_ru.md`;
- production-readiness:
  - `android:allowBackup="false"`;
  - лимит import backup-файла `10 MB`;
  - onboarding простым языком;
  - Google Play listing draft, screenshot plan, feature graphic brief и privacy
    policy обновлены под фактическое поведение приложения;
- финальная UX-правка формы записи перед beta:
  - обязательным явно считается только текст записи;
  - кнопка сохранения disabled, пока текст пустой;
  - `Текст цитаты *`, `Текст мысли *`, `Текст фрагмента *`;
  - остальные поля без слова `необязательно`;
  - секция темы упрощена до `Тема/тег` без длинного пояснения.
- store screenshots и финальная ручная проверка релизного APK/AAB.

## 1. К чему стремимся

MEMRYTH должен стать не просто списком цитат, а приватной офлайн-библиотекой личных текстовых фрагментов:

- пользователь быстро сохраняет цитаты, мысли и фрагменты;
- у каждой записи есть контекст: автор, источник, заметка, темы;
- сохраненное легко найти через поиск, фильтры, избранное и сортировки;
- данные хранятся локально, без обязательного аккаунта;
- пользователь понимает, как сохранить резервную копию и восстановить библиотеку.

Главный критерий продаваемого MVP: пользователь доверяет приложению свои данные и не боится их потерять.

## 2. Что уже есть

- Создание, редактирование и удаление записей.
- Типы записей: цитата, мысль, фрагмент.
- Автор, источник, заметка.
- Темы и избранное.
- Поиск по тексту, автору, источнику, заметке и темам.
- Поиск `#тема` как явный поиск только по темам.
- Фильтры по типам, темам и избранному.
- Индекс тем из верхней панели: все использованные темы, счетчики записей, сортировка по популярности или алфавиту, дерево для вложенных тем через `/`, переход в поиск по нажатию.
- Сортировки: новые, недавно измененные, старые, случайный порядок.
- Русский и английский интерфейс.
- Светлая и темная тема.
- Настройки чтения: тема, язык, плавный размер текста записи, плавный межстрочный интервал, показ заметки, показ автора и источника, сброс настроек.
- Локальное хранение через Hive.

## 3. Чего не хватает до честного релиза

Критично перед beta/internal testing:

1. Закрыть beta-smoke findings из `docs/beta_smoke_findings_ru.md`:
   - форма записи без внутреннего скролла многострочных полей;
   - взаимоисключающие фильтры типов;
   - реальное обновление `Случайного порядка`;
   - long press menu для одиночных действий и входа в массовый режим через
     пункт `Выбрать`;
   - осторожное массовое удаление через меню с подтверждением;
   - portrait-only для Android beta;
   - скрыть PIN/biometric из UI;
   - полезные подсказки в onboarding/about;
   - локализация Android quick add/widget;
   - слайдеры настроек без тяжелого сохранения на каждом движении.
2. Проверить импорт старых JSON backup после удаления лишнего отдельного поля
   источника.
3. Подготовить актуальные screenshots / feature graphic.
4. Финально проверить Google Play listing, privacy policy и data safety.

Важно после beta-feedback:

1. Реальный Play Console managed product `memryth_pro` и проверка
   purchase/restore.
2. Review Mode / Smart Resurfacing после первых beta-сигналов.
3. Collections / Projects, если появится понятный сценарий возврата
   к подборкам.

## 4. Ближайший порядок работ

### Этап 1. Архитектурная чистка

Статус: выполнено.

- `QuoteSortMode` вынесен в `lib/models/quote_sort_mode.dart`;
- убрать зависимость settings-слоя от controller-слоя;
- пустой `QuoteListViewModel` удален, `QuotesScreen` использует `QuoteController` напрямую;
- `lib/controllers` уже используется вместо старого пути;
- обновить импорты и docs;
- прогнать `dart format lib test`, `flutter analyze`, `flutter test`,
  `flutter build apk --release`, `flutter build appbundle`.

Результат: структура проекта станет чище перед дальнейшим ростом функций.

### Этап 2. Beta-smoke fixes

Сделать после ручного теста на телефоне:

- закрыть пункты из `docs/beta_smoke_findings_ru.md`, отмеченные как
  `Сделать перед beta`;
- оставить deferred-пункты в roadmap без расширения scope текущего beta;
- после правок снова пройти короткий smoke на телефоне.

Результат: beta перестает содержать найденные UX-блокеры и опасные сценарии
потери доступа к данным.

### Этап 3. Store preparation

Сделать:

- актуальные screenshots;
- feature graphic;
- финальная правка Google Play listing;
- privacy/data safety сверка с фактическим поведением приложения.

Результат: после покупки Google Play Developer account останется меньше
ручной подготовки.

### Этап 4. Релизная подготовка

Уже сделано:

- реальный package id и Android namespace: `app.memryth.android`;
- release signing с fallback на debug key для локальной сборки;
- release APK/AAB build;
- privacy policy и черновик Google Play listing.

Еще сделать:

- финальные store screenshots / feature graphic;
- ручная проверка release APK на устройстве перед загрузкой.

Результат: приложение технически готово к публикации.

## 5. Как реализовать экспорт

### Где разместить

Экспорт должен находиться в настройках, но не рядом с размером текста как еще одна мелкая визуальная настройка.

Лучше разделить настройки на секции:

1. `Данные`
   - Экспорт библиотеки
   - Импорт из файла
   - Восстановить backup
   - Дата последнего экспорта
2. `Чтение`
   - Размер текста записи
   - Межстрочный интервал
   - Тема
   - Язык
   - Показывать заметку на карточке
   - Показывать автора и источник
   - Сброс настроек
3. `Безопасность`
   - PIN/biometric lock, когда появится

Так пользователь быстро понимает, что backup/export относится к сохранности данных, а не к внешнему виду.

### Формат файла

Минимальный формат:

```json
{
  "app": "MEMRYTH",
  "schemaVersion": 1,
  "exportedAt": "2026-05-01T12:00:00.000Z",
  "tags": [],
  "quotes": []
}
```

В `tags` сохранять темы. Название поля пока лучше оставить `tags`, чтобы не ломать существующую модель Hive и будущую совместимость backup-файлов:

- `id`
- `name`

В `quotes` сохранять:

- `id`
- `text`
- `author`
- `tagIds`
- `typeKey`
- `createdAt`
- `updatedAt`
- `isFavorite`
- `sourceTitle`
- `note`

Важно сохранить `schemaVersion`, чтобы будущие версии приложения могли импортировать старые backup-файлы.

### Логика экспорта

Сервис:

- `ExportImportService`
- читает все темы и записи через репозитории;
- собирает JSON;
- сохраняет файл `memryth-backup-YYYY-MM-DD-HH-mm.json`;
- предлагает пользователю сохранить/поделиться файлом через системный Android sheet.

Для реализации на Flutter лучше использовать:

- `path_provider` для временного файла;
- `share_plus` для отправки/сохранения экспортированного файла;
- `file_picker` для выбора файла при импорте.

На Android лучше не строить экспорт вокруг прямого доступа к общей файловой системе и storage permissions. Надежнее использовать системный выбор/шаринг файла.

### Логика импорта

Первый вариант:

- пользователь выбирает `.json`;
- приложение проверяет `app`, `schemaVersion`, наличие `tags` и `quotes`;
- показывает экран подтверждения;
- предлагает режим:
  - `Объединить с текущей библиотекой`;
  - `Заменить текущую библиотеку`.

Для первого релиза можно начать с одного безопасного режима:

- `Объединить`.

Замену всей библиотеки лучше добавить после тестов, потому что это риск потери данных.

### Проверки перед импортом

Нужно проверять:

- файл действительно JSON;
- `schemaVersion` поддерживается;
- записи имеют `id` и `text`;
- даты корректно парсятся;
- темы без имени не импортируются;
- дубликаты тем по имени не создаются повторно;
- конфликтующие `id` не затирают существующие записи без явного решения.

## 6. Что делать с размером текста

Идея заменить "маленький / средний / большой" на сенсорную шкалу правильная, потому что сейчас это выглядит грубо и дешево.

Но не стоит делать slider от 1 до 40. Пользователь может выставить некрасивое значение и сломать читаемость.

Лучший вариант:

- плавный slider в узком читабельном диапазоне;
- подписи не как основной UI, а как короткое текущее значение;
- живой preview карточки;
- диапазон примерно: `18`-`28`;
- дефолт: `22`.

Для межстрочного интервала:

- тоже плавный slider в узком диапазоне;
- диапазон примерно: `1.25`-`1.65`;
- дефолт: `1.4`.

Размер интерфейса, плотность карточек и количество строк свернутой карточки не добавлять в настройки на этом этапе. Это делает экран тяжелее, а ценность для минимального продукта слабая.

## 7. Сброс настроек

Сброс нужен. Он должен быть внизу настроек, отдельно от обычных параметров.

Минимально:

- "Сбросить настройки интерфейса";
- подтверждение через dialog;
- сбрасываются тема, язык, размер текста, межстрочный интервал и preview-параметры карточек;
- пользовательские записи и темы не трогаются.

Текст должен ясно говорить:

`Записи, темы и заметки не будут удалены. Сбросятся только настройки внешнего вида и чтения.`

Удаление всей библиотеки не добавлять в первый релиз. Если когда-нибудь понадобится, оно должно быть отдельно, с очень жестким подтверждением.

## 8. Что делать бесплатным, а что Pro

Бесплатно:

- базовый export всей библиотеки;
- базовый import/restore;
- export selected;
- обычные batch actions;
- импорт только в безопасном режиме merge без перезаписи текущей библиотеки;
- сброс настроек;
- настройки чтения;
- создание и поиск записей;
- PIN/biometric lock;
- quick add shortcut;
- базовый quick-add widget;
- пустой приватный старт без автоматических demo-записей.

Pro:

- Review Mode;
- Smart Resurfacing;
- Collections / Projects;
- Beautiful Export / Share Cards;
- Backup History;
- Premium Reading / Focus Modes.

Причина: базовый export, import и PIN нельзя запирать за оплату.
Для приватного локального продукта это вопрос доверия, а не premium-роскошь.

Не добавлять в первый релиз:

- удаление всей библиотеки;
- режим импорта "заменить текущую библиотеку";
- автоматическое заполнение чужими demo-записями.

## 9. Будущий subscription-слой

Текущая коммерческая модель: `free + one-time Pro unlock`.

Подписку не добавлять, пока приложение остается полностью локальным и не
создает постоянных расходов на пользователя.

Отдельный subscription tier можно рассматривать позже, только если появятся:

- sync между устройствами;
- cloud backup;
- AI-функции;
- web clipper / browser extension;
- серверная обработка, хранение или регулярный внешний сервис.

Этот слой должен быть отдельным от локального Pro. Пользователь, купивший
one-time Pro, не должен терять локальные Pro-функции из-за появления подписки.

## 10. Следующий практический шаг

Блок "Данные" закрыт. Актуальный порядок после beta-smoke:

1. Production-readiness:
   - статус: кодовая часть закрыта;
   - остается снять реальные screenshots, создать feature graphic, опубликовать
     privacy policy URL и добавить support email.
2. Google Play Billing / Pro entitlement:
   - `in_app_purchase` добавлен;
   - локальное состояние Pro unlock заведено;
   - создать managed product `memryth_pro` в Play Console;
   - проверить purchase/restore на internal testing track.
3. Проверка спроса:
   - beta на 20-50 пользователей;
   - смотреть retention и реальные сохранения, а не только installs;
   - потом маленький тест Google Ads App Campaign.
4. Pro v1 после первых сигналов:
   - Review Mode;
   - Smart Resurfacing;
   - Collections / Projects;
   - Beautiful Export / Share Cards;
   - Backup History.
5. Будущий subscription-слой:
   - не делать до sync/cloud/AI/web clipper;
   - держать отдельно от локального one-time Pro.
6. Финальная релизная проверка:
   - `flutter analyze`;
   - `flutter test`;
   - `flutter build appbundle`;
   - загрузить в Play Console только свежий `app-release.aab`;
   - ручная проверка export/import, share-to-app, quick add.
